DATABASE_URL="file:../data/dev.db"
HOST="0.0.0.0"
PORT="3000"
SHUTDOWN_TIMEOUT_MS="10000"
AUTH_TOKEN_SECRET="change-this-local-secret"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="admin123"

# SQLite 备份：BACKUP_DIR 为备份目录，BACKUP_KEEP 为保留数量，
# BACKUP_ON_BOOT 控制在容器启动迁移前是否自动备份（1 开启 / 0 关闭）。
BACKUP_DIR="data/backups"
BACKUP_KEEP="5"
BACKUP_ON_BOOT="1"

# Docker Compose overrides DATABASE_URL, HOST and PORT internally.
# Set AUTH_TOKEN_SECRET to a long random value before a production deployment.
WEB_PORT="8080"
